'use client';

import CreateEditDialog from './create-edit-dialog/index';

/**
 * 面试创建/编辑弹窗组件导出入口
 *
 * @returns {JSX.Element} 创建/编辑弹窗组件
 */
export default function CreateEditDialogWrapper() {
  return <CreateEditDialog />;
}